package netty.serialize.impl;

import netty.serialize.Encoder;

public class KyroEncoder implements Encoder {
    @Override
    public byte[] encode(Object object) throws Exception {
        return new byte[0];
    }
}
